[package]
name = "media-factory"
version = "0.1.1"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
dialoguer = "0.11"
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
serde_json = "1"
reqwest = { version = "0.12", features = ["json", "rustls-tls", "stream"] }
tokio = { version = "1", features = ["full"] }
tokio-tungstenite = { version = "0.24", features = ["rustls-tls-native-roots"] }  # 火山播客 WebSocket API
futures-util = "0.3"  # WebSocket 流 .next()
axum = { version = "0.7", features = ["multipart"] }  # Web 服务（含文件上传）
flate2 = "1"  # 播客协议 gzip 压缩
uuid = { version = "1", features = ["v4"] }  # X-Api-Request-Id
async-trait = "0.1"
anyhow = "1"
thiserror = "1"
base64 = "0.22"
chrono = "0.4"
dirs = "5"

[target.'cfg(unix)'.dependencies]
libc = "0.2"  # serve 后台运行：setsid / kill 信号管理

[dev-dependencies]
wiremock = "0.6"
tempfile = "3"
